class Solution:
    def findMaxConsecutiveOnes(self, nums: list[int]) -> int:
        count=0
        longest=0
        for i in range(0,len(nums)):
            if nums[i]==1:
                count+=1
                longest=max(longest,count)
            else:
                count=0
